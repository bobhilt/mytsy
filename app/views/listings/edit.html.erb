<h1>Editing listing</h1>

<%= render 'form' %>

<%= link_to 'Show', @listing, class: "btn btn-link" %> |
<%= link_to 'Home', listings_path, class: "btn btn-link"%>
<%= "current_user == @listing.user: #{current_user == @listing.user}"%>